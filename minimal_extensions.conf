[general]
static=yes
writeprotect=no

[default]
exten => s,1,Answer()
same => n,Playback(demo-congrats)
same => n,Hangup()

exten => 1010,1,Answer()
same => n,Playback(demo-congrats)
same => n,Wait(3)
same => n,Hangup()

exten => 1000,1,Answer()
same => n,Playback(demo-congrats)
same => n,Wait(2)
same => n,Stasis(openai-voice-assistant)
same => n,Hangup()

exten => _X.,1,Answer()
same => n,Playback(demo-congrats)
same => n,Hangup()
